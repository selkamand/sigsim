% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_catalogues.R
\name{sig_bootstrap_catalogue}
\alias{sig_bootstrap_catalogue}
\title{Bootstrap a Mutation Catalogue}
\usage{
sig_bootstrap_catalogue(
  catalogue,
  n_catalogues = 10,
  format = c("sigverse", "matrix"),
  prefix = "catalogue",
  seed = NULL
)
}
\arguments{
\item{catalogue}{A single sigverse-style catalogue to bootstrap. See \code{\link[sigshared:catalogue]{sigshared::example_catalogue()}}.}

\item{n_catalogues}{The number of bootstrap replicates to generate. Default is 10.}

\item{format}{The output format. Either:
\itemize{
\item \code{"sigverse"}: return a list of sigverse-style data.frames (default).
\item \code{"matrix"}: return a matrix with rows as channels and columns as simulated catalogues.
}}

\item{prefix}{String prefix to name each catalogue. Default is \code{"catalogue"}.}

\item{seed}{Optional seed for reproducibility.}
}
\value{
Either a list of sigverse-style catalogues or a matrix of bootstrapped counts.
}
\description{
Generates bootstrap resamples of a mutational catalogue by multinomial resampling
of its observed mutation counts. This allows creation of synthetic catalogues that
reflect sampling variability in mutation observations.
}
\details{
The probability distribution for resampling is based on the \code{fraction} or \code{count} column
of the input catalogue. A total of \code{n_catalogues} bootstrapped catalogues are created,
each with the same total number of mutations as the original catalogue.
}
\examples{
library(sigshared)

# Bootstrap catalogue (return catalogue collection as list)
bootstraps <- sig_bootstrap_catalogue(example_catalogue(), n_catalogues = 5)

# Return  matrix
mx <- sig_bootstrap_catalogue(example_catalogue(), n_catalogues = 5, format = "matrix")

}
